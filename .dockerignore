# Git metadata
.git
.gitignore

# Logs and temp files
*.log
*.tmp
*.swp

# Node modules (they are handled per service)
node_modules/

# Build artifacts (frontend & backend will build inside their own Dockerfiles)
frontend/dist
backend/dist

# Env files / secrets (optional)
*.env
.env.*

# Docker-related files not needed inside build context
docker-compose.override.yml
docker-compose.prod.yml

# OS files
.DS_Store
Thumbs.db
